#include <stdio.h>
int main()
{
    int m,n,i,f=0;
    scanf("%d",&m);
scanf("%d",&n);
    for(i=m+1;i<n;++i)
    {
        if(%i==0)
        {
            f=1;
            break;
        }
    }

    if (f==0)
        printf("%d,n);
    else
        printf("%d",n);
    
    return 0;
}
